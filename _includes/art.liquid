---
layout: base.liquid
---
{% assign pics = collections.art %}
<a href="{{ collections.art[0].url }}" ">Test</a>
<p>{{ collections.art[0].data.name }}</p>
<div class="flex-container-column">
    <div class="art-label">
        <h3>{{ name }}</h3>
    </div>
    <div class="flex-container-column centered art"><img class="img-fluid" src="{{ path }}" alt="{{ name }}"/></div>
</div>
